// Copyright 2025 msawada

#ifndef ARRAY_HPP
#define ARRAY_HPP

class Array {
 private:

 public:
  Array();
  Array(const Array& other);
  Array& operator=(const Array& other);
  ~Array();
};

#endif
